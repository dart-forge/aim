name: aim_functions
description: "Cloud Functions for Firebase adapter: runs an Aim app as an HTTP function."
version: 0.1.0
repository: https://github.com/dart-forge/aim

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  aim_core: ^0.2.0
  firebase_functions: ^0.8.0
  shelf: ^1.4.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

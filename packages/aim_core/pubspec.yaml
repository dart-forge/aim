name: aim_core
description: Platform-independent core of the Aim web framework. Routing, middleware, request and response types with no dart:io dependency.
version: 0.3.0
repository: https://github.com/dart-forge/aim
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

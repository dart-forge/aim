name: aim_sqlite
description: A native SQLite driver for Dart. Talks to libsqlite3 over dart:ffi and runs statements off the event loop.
version: 0.1.0
repository: https://github.com/dart-forge/aim
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  aim_database: ^0.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

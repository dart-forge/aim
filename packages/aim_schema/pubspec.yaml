name: aim_schema
description: "Validate requests and declare responses with static types, without code generation."
version: 0.4.0
publish_to: none
repository: https://github.com/dart-forge/aim

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  aim_core: ^0.4.0

dev_dependencies:
  aim_server: ^0.4.0
  aim_server_testing: ^0.4.0
  lints: ^6.0.0
  path: ^1.9.0
  test: ^1.25.6

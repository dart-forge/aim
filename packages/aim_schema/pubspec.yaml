name: aim_schema
description: "Validate request data and read it back with static types, without code generation."
version: 0.4.0
repository: https://github.com/dart-forge/aim

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  aim_core: ^0.4.0

dev_dependencies:
  aim_server_testing: ^0.4.0
  lints: ^6.0.0
  test: ^1.25.6

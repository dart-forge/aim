name: aim_workers
description: "Cloudflare workerd adapter for Aim: runs an Aim app as a Worker."
version: 0.4.0
repository: https://github.com/dart-forge/aim

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  aim_core: ^0.4.0
  aim_edge: ^0.4.0
  web: ^1.1.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

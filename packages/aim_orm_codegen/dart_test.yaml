tags:
  integration:
    skip: "Needs Docker (the suite starts its own Postgres container via rig). Run with: dart test -t integration --run-skipped"
  slow:
    skip: "Runs build_runner in every golden fixture directory; takes minutes. Run with: dart test -t slow --run-skipped"

tags:
  integration:
    skip: "Needs Docker (the suites start their own Postgres containers via rig; a port is picked when the container is created and reused for its lifetime, not per run). Run with: dart test -t integration --run-skipped"

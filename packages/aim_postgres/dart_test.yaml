tags:
  integration:
    skip: "Needs Docker (the suites start their own Postgres containers on localhost, see test/integration/docker-compose.yml, ports 15433/15434/15435). Run with: dart test -t integration --run-skipped"

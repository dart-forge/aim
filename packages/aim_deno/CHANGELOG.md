## 0.4.0

Initial release. The Deno adapter, for Supabase Edge Functions and other
Deno-based runtimes: `serveDeno()`, string environment variables through
`c.env`, and `basePath` for runtimes that serve a function under a path.

name: bench_dart_io
description: Benchmark app on a bare dart:io HttpServer; the floor every framework is measured against.
publish_to: none
environment:
  sdk: ^3.13.0
dev_dependencies:
  lints: ^6.0.0

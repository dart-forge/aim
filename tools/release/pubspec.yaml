name: release
description: Release tools for Aim packages (bump, publish, create release).
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  yaml: ^3.1.3
  yaml_edit: ^2.2.2

dev_dependencies:
  test: ^1.25.6

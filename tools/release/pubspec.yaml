name: release
description: Aim-specific release steps that run alongside rask (aim_cli template pins, docs version, GitHub release).
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  test: ^1.25.6
